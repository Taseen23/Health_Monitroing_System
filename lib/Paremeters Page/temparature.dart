import 'package:flutter/material.dart';
class Temparature extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Temperature"),
      ),

    );
  }
}










